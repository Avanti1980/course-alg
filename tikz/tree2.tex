\documentclass{ctexart}
\usepackage{avanti-color}
%\usepackage{avanti-font}
\usepackage{avanti-math}
\usepackage{avanti-theorem}
\usepackage{avanti-others}

\RequirePackage{fontspec}
\xeCJKsetup{CJKmath=true}
\setCJKmainfont[BoldFont=FZHei-B01,ItalicFont=]{LXGW Bright}

\everymath{\color{Solarized-magenta}}
\pagestyle{empty} % 没有页眉和页脚

% define the plot style and the axis style
%\tikzset{font=\large}
\tikzset{base/.style={smooth, thick, Solarized-base01}}
\tikzset{arrow/.style={>=stealth, base}}

\begin{document}

\begin{tikzpicture}[scale = 1.0]
    
    \pgfmathsetmacro{\xinc}{1.0};
    
    \def \indexs {{1, 2, 3, 4, 5, "T"}};
    \def \names {{"\xv", "\av^{(1)}", "\av^{(2)}", "+", "\hat{y}"}};
    \def \styles {{"io", "hidden", "hidden", "add", "io"}};
    
    \foreach \i in {0, ..., 13}{
            \ifthenelse{\NOT \i = 10}{ % 第10个是省略号
                \node [base] (\i) at (\i*1, 0) {$\Theta(1)$}; % 画全部的 Theta(1)
                \draw [base, dashed] (\i) -- ++(0, 0.9); % 画上方虚线
            }{
                \node [base] (\i) at (\i*\xinc, 0) {$\cdots$};
            }
        }
    
    % 画大括号
    \draw[base, decorate, decoration={brace, raise=4pt}, Solarized-green] (13.4,-0.2) -- (-0.4,-0.2);
    
    \node [base] () at (6.5, -0.7) {$3^{\log_4 n} = n^{\log_4 3}$};
    
    \foreach \i in {0, ..., 8}{
        
            % 画全部的 c (n/16)^2
            \path [base] (\i*1.5+0.5, 2.0) node[draw=none,above] (root) {$c (\frac{n}{16})^2$}; 
            
            % 画下方三叉线
            \pgfmathsetmacro{\h}{-1.2};
            \draw [base] (root) -- ++(-0.5,\h);
            \draw [base] (root) -- ++(0,\h);
            \draw [base] (root) -- ++(0.5,\h);
        }
    
    \foreach \i in {0, ..., 2}{
        
            % 画全部的 c (n/4)^2
            \path [base] (\i*4.5+2, 3.5) node[draw=none,above] (root) {$c (\frac{n}{4})^2$};
            
            % 画下方三叉线
            \pgfmathsetmacro{\h}{-1.2};
            \draw [base] (root) -- ++(-1.5,\h);
            \draw [base] (root) -- ++(0,\h);
            \draw [base] (root) -- ++(1.5,\h);
        }
    
    \path [base] (6.5, 5.0) node[draw=none,above] (root) {$cn^2$};
    \pgfmathsetmacro{\h}{-1.15};
    \draw [base] (root) -- ++(-4.5, \h);
    \draw [base] (root) -- ++(0, \h);
    \draw [base] (root) -- ++(+4.5, \h);
    
    \draw [<->, arrow, Solarized-green] (-1, -0.2) -- node[fill=Solarized-base3] {$\log_4 n + 1$} (-1, 5.4);
    
\end{tikzpicture}

\end{document}