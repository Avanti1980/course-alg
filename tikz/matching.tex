\documentclass{ctexart}
\usepackage{avanti-color}
%\usepackage{avanti-font}
\usepackage{avanti-math}
\usepackage{avanti-theorem}
\usepackage{avanti-others}

\geometry{a2paper, left=2mm}

\RequirePackage{fontspec}
\xeCJKsetup{CJKmath=true}
\setCJKmainfont[BoldFont=FZHei-B01,ItalicFont=]{LXGW Bright}

\everymath{\color{Solarized-magenta}}
\pagestyle{empty} % 没有页眉和页脚

% define the plot style and the axis style
\tikzset{font=\large}

\tikzset{base/.style={smooth, very thick, Solarized-base01}}
\tikzset{point/.style={circle, minimum height=0.8cm, base, draw=Solarized-base01, fill=Solarized-base2}}
\tikzset{highlight/.style={base, line width=2mm, Solarized-blue, opacity=0.6}}

\begin{document}

\begin{center}
    \begin{tikzpicture}

        \pgfmathsetmacro{\l}{3.5};

        \node [point] (u1) at (0,0) {};
        \path (u1) ++(345:\l) node[point] (v1) {};
        \path (v1) ++(195:\l) node[point] (u2) {};
        \path (u2) ++(345:\l) node[point] (v2) {};
        \path (v2) ++(195:\l) node[point] (u3) {};
        \path (u3) ++(345:\l) node[point] (v3) {};
        \path (v3) ++(195:\l) node[point] (u4) {};
        \path (u4) ++(345:\l) node[point] (v4) {};
        \path (v4) ++(195:\l) node[point] (u5) {};

        \draw [highlight] (u1) -- (v1);
        \draw [highlight] (u3) -- (v3);

        \draw [base] (u1) -- (v1);
        \draw [base] (u2) -- (v1);
        \draw [base] (u2) -- (v3);
        \draw [base] (u3) -- (v2);
        \draw [base] (u3) -- (v3);
        \draw [base] (u3) -- (v4);
        \draw [base] (u4) -- (v3);
        \draw [base] (u5) -- (v3);

        \node [point] (u1) at (7,0) {};
        \path (u1) ++(345:\l) node[point] (v1) {};
        \path (v1) ++(195:\l) node[point] (u2) {};
        \path (u2) ++(345:\l) node[point] (v2) {};
        \path (v2) ++(195:\l) node[point] (u3) {};
        \path (u3) ++(345:\l) node[point] (v3) {};
        \path (v3) ++(195:\l) node[point] (u4) {};
        \path (u4) ++(345:\l) node[point] (v4) {};
        \path (v4) ++(195:\l) node[point] (u5) {};

        \draw [highlight] (u2) -- (v1);
        \draw [highlight] (u3) -- (v2);
        \draw [highlight] (u5) -- (v3);

        \draw [base] (u1) -- (v1);
        \draw [base] (u2) -- (v1);
        \draw [base] (u2) -- (v3);
        \draw [base] (u3) -- (v2);
        \draw [base] (u3) -- (v3);
        \draw [base] (u3) -- (v4);
        \draw [base] (u4) -- (v3);
        \draw [base] (u5) -- (v3);

    \end{tikzpicture}
\end{center}


\end{document}
