\documentclass[tikz]{standalone}
\usepackage{avanti-tikz4slides}

% define the plot style and the axis style
\tikzset{font=\LARGE}
\tikzset{base/.style = {smooth,very thick,draw=SolarizedBase01,SolarizedBase01}}
\tikzset{arrow/.style={->, -{Stealth[scale=1.0]}, base, draw=SolarizedBlue}}
\tikzset{arrow2/.style={<->, {Stealth[scale=1.0]}-{Stealth[scale=1.0]}, base, draw=SolarizedGreen}}

\begin{document}

\begin{tikzpicture}[
    base,
    level distance=80,
    level 1/.style = {sibling distance=225},
    level 2/.style = {sibling distance=75},
    level 3/.style = {sibling distance=25},
  ]
  \node (Root) [] {$c n^2$}
  child {node (Root-1-1) [] {$c (\frac{n}{4})^2$}
      child {node (Root-2-1) [] {$c (\frac{n}{16})^2$}
          child {node (Root-3-1) [] {$\vdots$}}
          child {node (Root-3-2) [] {$\vdots$}}
          child {node (Root-3-3) [] {$\vdots$}}
        }
      child {node (Root-2-2) [] {$c (\frac{n}{16})^2$}
          child {node (Root-3-4) [] {$\vdots$}}
          child {node (Root-3-5) [] {$\vdots$}}
          child {node (Root-3-6) [] {$\vdots$}}
        }
      child {node (Root-2-3) [] {$c (\frac{n}{16})^2$}
          child {node (Root-3-7) [] {$\vdots$}}
          child {node (Root-3-8) [] {$\vdots$}}
          child {node (Root-3-9) [] {$\vdots$}}
        }
    }
  child {node (Root-1-2) [] {$c (\frac{n}{4})^2$}
      child {node (Root-2-4) [] {$c (\frac{n}{16})^2$}
          child {node (Root-3-10) [] {$\vdots$}}
          child {node (Root-3-11) [] {$\vdots$}}
          child {node (Root-3-12) [] {$\vdots$}}
        }
      child {node (Root-2-5) [] {$c (\frac{n}{16})^2$}
          child {node (Root-3-13) [] {$\vdots$}}
          child {node (Root-3-14) [] {$\vdots$}}
          child {node (Root-3-15) [] {$\vdots$}}
        }
      child {node (Root-2-6) [] {$c (\frac{n}{16})^2$}
          child {node (Root-3-16) [] {$\vdots$}}
          child {node (Root-3-17) [] {$\vdots$}}
          child {node (Root-3-18) [] {$\vdots$}}
        }
    }
  child {node (Root-1-3) [] {$c (\frac{n}{4})^2$}
      child {node (Root-2-7) [] {$c (\frac{n}{16})^2$}
          child {node (Root-3-19) [] {$\vdots$}}
          child {node (Root-3-20) [] {$\vdots$}}
          child {node (Root-3-21) [] {$\vdots$}}
        }
      child {node (Root-2-8) [] {$c (\frac{n}{16})^2$}
          child {node (Root-3-22) [] {$\vdots$}}
          child {node (Root-3-23) [] {$\vdots$}}
          child {node (Root-3-24) [] {$\vdots$}}
        }
      child {node (Root-2-9) [] {$c (\frac{n}{16})^2$}
          child {node (Root-3-25) [] {$\vdots$}}
          child {node (Root-3-26) [] {$\vdots$}}
          child {node (Root-3-27) [] {$\vdots$}}
        }
    };

  % 画叶结点
  \pgfmathsetmacro{\shifty}{0.5};
  \foreach \i in {3,5,7}
    {
      \pgfmathsetmacro{\j}{3 * \i - 2};
      \path [base] (Root-3-\j) node [below=\shifty] () {$\Theta(1)$};
      \pgfmathsetmacro{\j}{3 * \i};
      \path [base] (Root-3-\j) node [below=\shifty] () {$\Theta(1)$};
    }
  \path [base] (Root-3-1) node [below=\shifty] (Root-4-1) {$\Theta(1)$};
  \path [base] (Root-3-3) node [below=\shifty] () {$\Theta(1)$};
  \path [base] (Root-3-25) node [below=\shifty] () {$\Theta(1)$};
  \path [base] (Root-3-27) node [below=\shifty] (Root-4-27) {$\Theta(1)$};

  \foreach \i in {2,4,6,8}
    {
      \pgfmathsetmacro{\j}{3 * \i - 1};
      \path [base] (Root-3-\j) node [below=0.6cm] () {$\cdots$};
    }

  % 画每行的和
  \pgfmathsetmacro{\shiftx}{1.5};
  \pgfmathsetmacro{\shiftxx}{3};
  \pgfmathsetmacro{\shiftxxx}{0.2};
  \path (Root) ++(\shiftx,0) coordinate (tail);
  \path (Root -| Root-4-27) ++(\shiftxx,0) coordinate (head);
  \draw [arrow] (tail) -- (head);
  \path (head) node [right=\shiftxxx] {$c n^2$};

  \path (Root-1-3) ++(\shiftx,0) coordinate (tail);
  \path (Root-1-3 -| Root-4-27) ++(\shiftxx,0) coordinate (head);
  \draw [arrow] (tail) -- (head);
  \path (head) node [right=\shiftxxx] {$\frac{3}{16} c n^2$};

  \path (Root-2-9) ++(\shiftx,0) coordinate (tail);
  \path (Root-2-9 -| Root-4-27) ++(\shiftxx,0) coordinate (head);
  \draw [arrow] (tail) -- (head);
  \path (head) node [right=\shiftxxx] {$(\frac{3}{16})^2 c n^2$};

  \path (Root-4-27) ++(\shiftx,0) coordinate (tail);
  \path (Root-4-27) ++(\shiftxx,0) coordinate (head);
  \draw [arrow] (tail) -- (head);
  \path (head) node [right=\shiftxxx] {$\Theta(n^{\log_4 3})$};

  % 最下面的括号
  \path (Root-4-1) ++(-0.5,0) coordinate (tail);
  \path (Root-4-27) ++(0.5,0) coordinate (head);
  \draw [base,SolarizedYellow,decorate,decoration={brace,amplitude=12pt,mirror,raise=12pt}] (tail) -- (head) node[midway,yshift=-35] {$3^{\log_4 n} = n^{\log_4 3}$};

  % 最左面的双箭头
  \path (Root -| Root-4-1) ++(-1.2,0) coordinate (head);
  \path (Root-4-1) ++(-1.2,0) coordinate (tail);
  \draw [arrow2] (tail) -- (head) node [above=0pt, sloped, pos=0.5] {共$\log_4 n + 1$ 层};

\end{tikzpicture}

\end{document}